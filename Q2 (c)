void min(node* root){
    if(root==NULL){
        cout<<"Empty tree"<<endl;
        return;
    }
    while(root->left!=NULL){
        root=root->left;
    }
    cout<<"Min element is "<<root->data<<endl;
    return;
}
